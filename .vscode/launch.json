{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Ejecutar Simple DB",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "preLaunchTask": "npm: lint"
    }
  ]
}
