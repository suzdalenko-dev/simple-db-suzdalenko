SIMPLE DB 0.1.1 — DELIVERY PLAN

1. JavaScript architecture
   [x] Remove TypeScript, tsconfig, and compilation.
   [x] Activate directly from src/extension.js.
   [x] Native adapters/core/managers/services/sql/storage/ui/views/test structure.

2. Five database engines
   [x] SQLite.
   [x] PostgreSQL.
   [x] MySQL.
   [x] SQL Server.
   [x] Oracle.

3. Connections
   [x] Create/edit/test/delete profiles.
   [x] SecretStorage for passwords.
   [x] Simultaneous pools/connections, connection states, and safe disconnect.
   [x] Per-editor database/schema context.

4. Explorer
   [x] Databases/schemas/tables/views/columns/routines.
   [x] Materialized views, indexes, triggers, sequences, and types.
   [x] Oracle packages, synonyms, and events where supported.
   [x] Object definitions and qualified names.

5. SQL and DDL
   [x] Document/selection/current-statement execution.
   [x] PostgreSQL $$, MySQL DELIMITER/#, SQL Server GO, Oracle PL/SQL/q-quotes, SQLite triggers.
   [x] Arbitrary DML/DDL.
   [x] CREATE/ALTER/DROP templates and DDL inspection.
   [x] DROP/TRUNCATE confirmation and UPDATE/DELETE-without-WHERE warning.

6. Transactions/cancellation
   [x] Per-editor BEGIN/COMMIT/ROLLBACK.
   [x] Physical connection reserved for each transaction.
   [x] PostgreSQL/MySQL/SQL Server/Oracle/SQLite cancellation.
   [x] Timeout and rollback-required state.

7. Results
   [x] Driver streaming/cursor/result-set support.
   [x] maxRows=0 with no limit by default.
   [x] Temporary paging without added TOP/LIMIT/FETCH.
   [x] Multiple result sets, NULL, types, timings, and row counts.
   [x] Copy cell/row/selection.
   [x] CSV/JSON/XLSX export.
   [x] Configurable history.

8. Quality and delivery
   [x] JavaScript ESLint.
   [x] JavaScript tests, including SQLite integration.
   [x] Final npm run check validation (50 tests).
   [x] npm audit with no vulnerabilities.
   [x] VSIX packaging validated as a ZIP archive.
   [x] Verify that no TypeScript source/configuration remains in the source tree or VSIX.
   [x] Final version-0.1.1 commit.
   [x] Publish version-0.1.1 branch to GitHub.
